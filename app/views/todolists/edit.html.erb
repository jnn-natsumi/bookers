<h1>edit page</h1>
<%= form_for(@list, url:"/todolists/#{@list.id}") do |f| %>
    <h4>title</h4>
    <%= f.text_field :title %>
    <h4>body</h4>
    <%= f.text_area :body %>
    <%= f.submit '保存' %>
<% end %>